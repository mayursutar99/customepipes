import { SentencereducerPipe } from './sentencereducer.pipe';

describe('SentencereducerPipe', () => {
  it('create an instance', () => {
    const pipe = new SentencereducerPipe();
    expect(pipe).toBeTruthy();
  });
});
